"  @override
  Widget build(BuildContext context) {
    return FutureBuilder<String>(
      future: Executable.getOSName(),
      builder: (BuildContext context, AsyncSnapshot<String> osname) {
        if (osname.connectionState == ConnectionState.waiting) {
          return CircularProgressIndicator();
        } else {
          return Scaffold(
      appBar: AppBar(
        title: Text('${osname.data} Tour'),
      ),
      body: Stepper(
        steps: _buildSteps(),
        currentStep: _currentStep,
        onStepContinue: () {
          if (_currentStep < _buildSteps().length - 1) {
            setState(() {
              _currentStep++;
            });
          }
        },
        onStepCancel: () {
          if (_currentStep > 0) {
            setState(() {
              _currentStep--;"

Please copy the above code, but add the missing endings.




The code snippet you provided is missing the closing brackets for the setState, onStepCancel, Stepper, Scaffold, builder, and FutureBuilder functions. Here is the corrected version:

@override
Widget build(BuildContext context) {
  return FutureBuilder<String>(
    future: Executable.getOSName(),
    builder: (BuildContext context, AsyncSnapshot<String> osname) {
      if (osname.connectionState == ConnectionState.waiting) {
        return CircularProgressIndicator();
      } else {
        return Scaffold(
          appBar: AppBar(
            title: Text('${osname.data} Tour'),
          ),
          body: Stepper(
            steps: _buildSteps(),
            currentStep: _currentStep,
            onStepContinue: () {
              if (_currentStep < _buildSteps().length - 1) {
                setState(() {
                  _currentStep++;
                });
              }
            },
            onStepCancel: () {
              if (_currentStep > 0) {
                setState(() {
                  _currentStep--;
                });
              }
            },
          ),
        );
      }
    },
  );
}

This code now correctly closes all the functions and widgets.









"ERROR: lib/screens/steps.dart:14:50: Error: Can't find '}' to match '{'.
ERROR: class _StepScreenState extends State<StepScreen> {
ERROR:                                                  ^
ERROR: Target kernel_snapshot failed: Exception
Building Linux application...                                           
Build process failed
"





The error message indicates that there is a missing closing bracket } in your [StepScreenState](file:///home/daniella/firstlogon_tour/lib/screens/steps.dart#11%2C3-11%2C3) class in the steps.dart file. This is causing the Dart compiler to fail when trying to build the application.

To fix this, you need to ensure that all your methods, functions, and class definitions are properly closed with the } character.

Unfortunately, without the full code of the _StepScreenState class, I can't provide a precise solution. However, the general structure of a class in Dart should look like this:

class _StepScreenState extends State<StepScreen> {
  // Class variables and methods here

  @override
  Widget build(BuildContext context) {
    // Build method implementation here
  }
}

Please check your _StepScreenState class and make sure that you have the correct number of opening { and closing } brackets.
